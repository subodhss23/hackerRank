// print all N integers in A in reverse order as a single line
// of space-separated integers.

let array = [1,2,3,4,5,5];

function reveseArr(array){
    console.log(array.reverse());
}
reveseArr(array);